pipeline {
  	agent {label ""}
		stages {
			stage("test1"){
				steps {
					sh "ls -la"
					}
				}
			stage("test2") {
				steps {
					sh "echo 'hello world' > text.txt"
					sh "ls -la && cat text.txt"
				}
			}	
		}
	}

