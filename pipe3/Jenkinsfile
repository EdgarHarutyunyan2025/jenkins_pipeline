pipeline {
	agent {
		docker {
			image "python:latest"
			args "--user root"
		}
	}
	stages {
		stage("pylint test") {
			steps{
				sh "ls -la"
				sh "pip install pylint"
				sh "pylint pipe3/app.py  --fail-under=8.0"
			}
		}
		stage("build docker image") {
			steps{
				sh "docker ps"
				sh "docker build -t Jenkins_pylint_test:v1 ./app/"
				sh "docker images"
			}
		}
	}
}
